/**
 * Arithmetic operators
 */

let a = 15;
let b = 4;
let c = 3;

console.log(`let a: ${a} (${typeof a})`);
console.log(`let b: ${b} (${typeof b})`);
console.log(`let c: ${c} (${typeof c})`);

let result = a % b;

/**
 * Addition
 * let a = 15;
   let b = "4";
   let c = 3;

   let result = a + b;
   console: 154 

   Be careful with addition, while JS will still read "4" as the number when we do subtraction,
   divination, and multipling, it will act weird with PLUS.
 */

console.log("Result", result);

